<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SolarWin.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <StackPanel
            MaxWidth="560"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="16">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind ViewModel.Greeting, Mode=OneWay}" />
            <TextBlock
                Opacity="0.75"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="{x:Bind ViewModel.AccountSummary, Mode=OneWay}" />
            <TextBlock
                Opacity="0.6"
                Text="客户端骨架已就绪：API 模型、HttpClient、Padlock 登录与 PasswordVault 已接入。"
                TextWrapping="Wrap" />
            <Button
                HorizontalAlignment="Left"
                Command="{x:Bind ViewModel.LogoutCommand}"
                Content="退出登录" />
        </StackPanel>
    </Grid>
</Page>
