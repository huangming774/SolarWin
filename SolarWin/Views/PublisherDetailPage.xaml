<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SolarWin.Views.PublisherDetailPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SolarWin.ViewModels"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Padding="12,8" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Click="BackButton_OnClick" Content="返回" />
            <TextBlock
                Grid.Column="1"
                VerticalAlignment="Center"
                Style="{StaticResource SubtitleTextBlockStyle}"
                Text="{x:Bind ViewModel.Title, Mode=OneWay}" />
            <ProgressRing Grid.Column="2" Width="24" Height="24" IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" />
        </Grid>

        <ScrollViewer Grid.Row="1" Padding="24,8,24,24">
            <StackPanel MaxWidth="720" Spacing="14">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid Width="72" Height="72">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12" />
                        <Border CornerRadius="12">
                            <Border.Background>
                                <ImageBrush ImageSource="{x:Bind ViewModel.PictureImage, Mode=OneWay}" Stretch="UniformToFill" />
                            </Border.Background>
                        </Border>
                    </Grid>
                    <StackPanel Grid.Column="1" Spacing="4">
                        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="{x:Bind ViewModel.Title, Mode=OneWay}" />
                        <TextBlock Opacity="0.75" Text="{x:Bind ViewModel.Handle, Mode=OneWay}" />
                        <TextBlock Opacity="0.7" Text="{x:Bind ViewModel.Meta, Mode=OneWay}" TextWrapping="Wrap" />
                        <TextBlock Opacity="0.75" Text="{x:Bind ViewModel.StatsText, Mode=OneWay}" TextWrapping="Wrap" />
                        <TextBlock Opacity="0.75" Text="{x:Bind ViewModel.SubscriptionText, Mode=OneWay}" />
                        <TextBlock Opacity="0.75" Text="{x:Bind ViewModel.MembershipText, Mode=OneWay}" />
                    </StackPanel>
                </Grid>

                <TextBlock Text="{x:Bind ViewModel.Bio, Mode=OneWay}" TextWrapping="WrapWholeWords" />
                <TextBlock Foreground="{ThemeResource SystemFillColorCriticalBrush}" Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" TextWrapping="Wrap" />

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Command="{x:Bind ViewModel.SubscribeCommand}" Content="订阅" Style="{StaticResource AccentButtonStyle}" />
                    <Button Command="{x:Bind ViewModel.UnsubscribeCommand}" Content="退订" />
                    <Button Command="{x:Bind ViewModel.OpenPublisherFeedCommand}" Content="看帖" />
                    <Button Command="{x:Bind ViewModel.LoadCommand}" Content="刷新" />
                    <Button Command="{x:Bind ViewModel.LeaveCommand}" Content="退出成员" />
                </StackPanel>

                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="编辑资料" />
                <TextBox PlaceholderText="显示名" Text="{x:Bind ViewModel.EditNick, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox MinHeight="72" AcceptsReturn="True" PlaceholderText="简介" Text="{x:Bind ViewModel.EditBio, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" />
                <Button Command="{x:Bind ViewModel.SaveProfileCommand}" Content="保存" />

                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="成员" />
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBox Width="280" PlaceholderText="邀请用户 GUID" Text="{x:Bind ViewModel.InviteUserId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Command="{x:Bind ViewModel.InviteMemberCommand}" Content="邀请" />
                </StackPanel>
                <ListView MaxHeight="220" ItemsSource="{x:Bind ViewModel.Members, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:SocialListItemViewModel">
                            <Grid Padding="4,8" ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" Text="{x:Bind Title}" />
                                    <TextBlock Opacity="0.7" Text="{x:Bind Subtitle}" />
                                    <TextBlock Opacity="0.55" Style="{StaticResource CaptionTextBlockStyle}" Text="{x:Bind Meta}" />
                                </StackPanel>
                                <Button Grid.Column="1" Content="移除"
                                        Command="{Binding ElementName=RootPage, Path=ViewModel.RemoveMemberCommand}"
                                        CommandParameter="{x:Bind}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Features" />
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBox Width="200" PlaceholderText="flag" Text="{x:Bind ViewModel.NewFeatureFlag, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Command="{x:Bind ViewModel.AddFeatureCommand}" Content="添加" />
                </StackPanel>
                <ListView MaxHeight="140" ItemsSource="{x:Bind ViewModel.Features, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:SocialListItemViewModel">
                            <StackPanel Padding="4,6" Orientation="Horizontal" Spacing="12">
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind Title}" />
                                <TextBlock Opacity="0.7" Text="{x:Bind Subtitle}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="合集" />
                <ListView MaxHeight="180" ItemsSource="{x:Bind ViewModel.Collections, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:SocialListItemViewModel">
                            <Grid Padding="4,8" ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" Text="{x:Bind Title}" />
                                    <TextBlock Opacity="0.7" Text="{x:Bind Meta}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                                    <Button Content="看帖"
                                            Command="{Binding ElementName=RootPage, Path=ViewModel.OpenCollectionFeedCommand}"
                                            CommandParameter="{x:Bind}"
                                            Style="{StaticResource AccentButtonStyle}" />
                                    <Button Content="订阅"
                                            Command="{Binding ElementName=RootPage, Path=ViewModel.SubscribeCollectionCommand}"
                                            CommandParameter="{x:Bind}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
